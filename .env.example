# Prisma / PostgreSQL
DATABASE_URL=

# Public URL of the hosted japan.pmtiles archive
NEXT_PUBLIC_MAP_PMTILES_URL=

# ポートフォリオ公開時、投稿位置を日本国内のランダムな座標に差し替える(true/未設定)
# NEXT_PUBLIC_*はビルド時に埋め込まれるため、値を変更したら再デプロイ(再ビルド)が必要
NEXT_PUBLIC_DEMO_MODE=

# 認証(NextAuth等を導入する場合)
# AUTH_SECRET=
# AUTH_URL=
